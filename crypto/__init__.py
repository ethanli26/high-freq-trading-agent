"""Crypto asset class on the existing backtest engine (hype-momentum pilot).

This package adds crypto data, a small-coin universe, a brutal cost model, and a
hype-momentum strategy that all plug into the SAME walk-forward engine, risk sizing,
and benchmark code used for equities — a new asset class, not a reimplementation.

Read-only research. No live orders.
"""
